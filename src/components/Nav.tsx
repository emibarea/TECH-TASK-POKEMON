import { IonHeader, IonToolbar, IonTitle } from "@ionic/react";

const Navbar: React.FC = () => {
  return (
    <IonHeader>
      <IonToolbar>
        <IonTitle>TECH TASK - POKEAPI</IonTitle>
      </IonToolbar>
    </IonHeader>
  );
};

export default Navbar;
